# Database
DATABASE_URL="file:./dev.db"

# NextAuth.js
NEXTAUTH_SECRET="your-nextauth-secret-key-here"
# For local development:
NEXTAUTH_URL="http://localhost:2137"
# For mobile testing on local network, use your local IP address:
# NEXTAUTH_URL="https://carrie-unvisitable-kiddingly.ngrok-free.dev"
# See MOBILE_TESTING.md for detailed instructions

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Email Configuration (for password reset)
EMAIL_SERVER_HOST="smtp.gmail.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="your-email@gmail.com"
EMAIL_SERVER_PASSWORD="your-app-password"
EMAIL_FROM="noreply@unicornclimb.app"

# Supabase (when using Supabase)
SUPABASE_URL="your-supabase-url"
SUPABASE_ANON_KEY="your-supabase-anon-key"

# Google Calendar API (for later)

# Web Push / VAPID Keys (for push notifications)
# Generate keys with: node scripts/generate-vapid-keys.js
VAPID_PUBLIC_KEY="your-vapid-public-key"
VAPID_PRIVATE_KEY="your-vapid-private-key"
VAPID_SUBJECT="mailto:your-email@example.com"
